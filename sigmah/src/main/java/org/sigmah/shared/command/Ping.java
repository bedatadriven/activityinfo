package org.sigmah.shared.command;

import org.sigmah.shared.command.result.VoidResult;

/**
 * Command that simply pings the server.
 * Used to detect connection
 * 
 */
public class Ping implements Command<VoidResult> {
	
	

}
